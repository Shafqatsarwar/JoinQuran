# JoinQuran Website Commands

This file contains the necessary commands to set up and run the JoinQuran website project.

## Prerequisites

*   Node.js (v18 or later)
*   npm

## Installation

1.  Navigate to the `app` directory:
    ```bash
    cd app
    ```
2.  Install the dependencies:
    ```bash
    npm install
    ```

## Running the Development Server

1.  Navigate to the `app` directory:
    ```bash
    cd app
    ```
2.  Run the development server:
    ```bash
    npm run dev
    ```
3.  Open [http://localhost:3000](http://localhost:3000) in your browser to see the result.

## Building for Production

1.  Navigate to the `app` directory:
    ```bash
    cd app
    ```
2.  Build the project:
    ```bash
    npm run build
    ```
3. ## Test prompts
come on my friend test all filese 1 by 1 carefully, no changes condition in pwa files  trigger prompt etc and gemini model 2.5-flash or pro, and with out any major changes fix test and deploy to vercel:
1.packages dependecies crashing missing miss matchin fix them any buid issue
2. email signup send email from exellence to onlinequran50, env veriable set already
3. some buttons and pages having color cotrast issue, if back ground colour is light then text should gray or black, if back ground colour is dark then text should white or golden 
4. ui having trouble in start up specially, reverse it back to our initial 1, we fixed in begginig, scree shorts for example attached: 